apiVersion: apps/v1
kind: Deployment
metadata:
  name: tiles-inspire
spec:
  replicas: 1
  selector:
    matchLabels:
      app: tiles-inspire
  template:
    metadata:
      labels:
        app: tiles-inspire
    spec:
      containers:
      - name: martin
        image: ghcr.io/maplibre/martin
        ports:
        - containerPort: 3000
        env:
        - name: DATABASE_URL
          valueFrom:
            secretKeyRef:
              name: db-secrets
              key: REMOTE_INSPIRE_URL
        - name: WATCH_MODE
          value: "true"
        command:
        - martin
        - --auto-bounds
        - calc
        - --keep-alive
        - "200"
        - --pool-size
        - "50"
        - --default-srid
        - "4326"
